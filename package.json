{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx electron ./src --enable-logging",
    "build": "npx electron-packager src pmdro-timer --platform=darwin --arch=x64 --overwrite"
  },
  "devDependencies": {
    "electron": "^15.1.2",
    "electron-packager": "^15.4.0"
  },
  "private": true
}
